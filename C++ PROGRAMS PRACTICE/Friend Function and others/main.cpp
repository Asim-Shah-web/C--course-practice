#include <iostream>
#include "Hello.h"
/* run this program using the console pauser or add your own getch, system("pause") or input loop */
using namespace std;
int main() {
	Hello obj1;
	obj1.setValue(50);
	obj1.display();
	return 0;
}
